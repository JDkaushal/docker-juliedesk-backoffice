.messages-thread-info-panel {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  width: 350px;

  border-left: 1px solid #ccc;
  padding: 60px 10px 10px 10px;
  z-index: 2;
  background-color: #fff;

  z-index: 10;

  &.scrollable {
    overflow: scroll;
  }

  &.frozen {
    overflow: unset;
  }

  .only-locale-panel {
    display: none;
  }

  .classic-info-panel {

  }

  .message-container {
    position: fixed;
    top: 0px;
    right: 0px;
    width: 350px;
    background: $darkBlue;
    padding: 5px 0;
    text-align: center;
    color: #fff;
    font-size: 22px;
    font-weight: 200;
    line-height: 30px;
    display: none;
    z-index: 2;
  }

  #thread-header-other-entries-container {

    .new-events-container {
      border-bottom: 2px solid #ccc;
      margin: 0 -10px 20px -10px;
      padding: 10px;
      position: relative;
      z-index: 1;

      .new-event-tiles-container {

      }

      .new-event-tile-container {
        padding: 0 0 30px 0;
      }
    }

    .create-new-event-tile-button {
      display: block;
      margin: 20px auto;
      width: 200px;
      @include border-radius(2px);
    }

    #ticket_module {
      margin: 20px auto;
      width: 260px;

      .sprite {
        background-image: url(<%= asset_path 'SNCF.jpg' %>);
        background-repeat: no-repeat;
        background-size: 50px 50px;
        width: 50px;
        display: inline-block;
        height: 50px;
        position: relative;
        top: 16px;
      }

      .pre-button {
        margin-left: 5px;
      }

      .ticket-parsing-btn {
        margin-left: 7px;
        display: inline-block;
        background-color: $aiControlColor;
        color: white;
      }

    }

    .selected-events-list-container {
      border-bottom: 1px solid #ccc;
      background-color: #fff;
      margin: -60px -10px 20px -10px;

      .selected-events-list {
        padding: 10px 10px 10px 10px;
      }
      .selected-events-title {
        color: $darkBlue;
        margin: 10px 0 20px;
        text-align: center;
        font-size: 18px;
        font-weight: 200;
      }
      .selected-event {
        background: $darkBlue;
        color: white;
        text-align: left;
        padding: 10px 10px 10px 30px;
        margin: 10px 0;

        .selected-event-title {
          font-size: 15px;
        }
        .selected-event-date {
          font-size: 12px;
        }
        .selected-event-no-attendees-container {
          background: white;
          margin: 10px 0 0 0;
          position: relative;

          .selected-event-no-attendees {
            font-weight: 600;
            color: #e00;
            display: inline-block;
            padding: 3px 10px;
            font-size: 12px;
          }
          .selected-event-force-cancel {
            position: absolute;
            top: 0px;
            right: 0px;
            height: 23px;
            color: #444;
            padding: 3px 10px;
            font-size: 12px;
            input[type=checkbox] {
              margin: 0 5px;
            }
          }
        }
      }
    }

    .time-slots-to-suggest-list-container {
      //position: fixed;
      /*top: 40px;*/
      /*width: 350px;*/
      /*right: 0;*/
      /*border-bottom: 1px solid #ccc;*/
      /*border-left: 1px solid #ccc;*/
      background-color: #fff;
      z-index: 2;

      .time-slots-to-suggest-list {
        padding: 10px 10px 30px 10px;

        .dates-suggestions-from-ai-remaining-warning {
          color: $aiControlColor;
          text-align: center;
        }

        .dates-suggestion-timezone-label {
          font-size: 11px;
          opacity: 0.8;
          margin: 10px 0 0 0;
          color: #AAAAAA;
        }

        #dates_suggestion_timezone {
          width: 100%;
          height: 30px;
        }

        .dates-suggestions-wrapper {
          margin-top: 10px;
          //max-height: 500px;
          //overflow-y: auto;

          .timezoned-suggestions-area {
            margin-bottom: 30px;

            .timezone-title {
              font-weight: bold;
              margin-bottom: 5px;
            }

            .btn {
              margin-top: 2px;
              font-size: 15px;
              border-radius: 0;
              text-align: left;
            }

            .from-ai {
              background-color: $aiControlColor;
              border-color: $aiControlInputBorderColor;
              padding: 0;

              &:hover {
                background-color: $aiEventsHighlightedColor;
              }

              &.highlighted {
                background-color: $aiEventsHighlightedColor;
              }

              .date-text {
                padding: 6px 12px;
              }
            }

            .ai-suggestion-action-wrapper {
              /*position: absolute;*/
              /*right: 40px;*/
              border-top: solid 1px;
              height: 30px;

              .ai-suggestion-action-item-container {
                display: inline-block;
                width: 50%;
                text-align: center;
                height: 100%;

                &.highlight {
                  background-color: white;
                  .ai-suggestion-action {

                    &.accept {
                      background-image: url(<%= asset_path 'validate_purple.png' %>);
                    }

                    &.reject {
                      background-image: url(<%= asset_path 'unvalidate_purple.png' %>);
                    }
                  }
                }

                &:hover {
                  background-color: white;
                  .ai-suggestion-action {

                    &.accept {
                      background-image: url(<%= asset_path 'validate_purple.png' %>);
                    }

                    &.reject {
                      background-image: url(<%= asset_path 'unvalidate_purple.png' %>);
                    }
                  }
                }

                &:first-child {
                  border-right: solid 1px;
                }

                &:last-child {
                  margin-left: -4px;
                }
              }

              .ai-suggestion-action {
                display: inline-block;
                height: 16px;
                position: relative;
                width: 16px;
                top: 25%;

                &.accept {
                  @include ai-date-suggestion-sprite-accept();
                }

                &.reject {
                  @include ai-date-suggestion-sprite-reject();
                }
              }
            }
          }
        }
      }

      .ai-area {
        color: $aiControlColor;
        font-weight: bold;

        .ai-loader-area {
          .ai-loader {
            background-image: url(<%= asset_path 'ai_processing_loader.gif' %>);
            background-repeat: no-repeat;
            background-size: 25px 25px;
            width: 25px;
            display: inline-block;
            height: 25px;
            position: relative;
            top: 6px;
          }

          .loader-text {
            margin-left: 5px;
          }
        }

        .warning-area {
          color: $aiControlColor;

          .ai-warning-sprite {
            background-image: url(<%= asset_path 'warning2x.png' %>);
            background-repeat: no-repeat;
            background-size: 25px 25px;
            width: 25px;
            display: inline-block;
            height: 25px;
            position: relative;
            top: 6px;
          }

          .ai-warning {
            margin-left: 5px;
          }
        }

        .error-area {
          color: red;

          .ai-error-sprite {
            background-image: url(<%= asset_path 'warning_red2x.png' %>);
            background-repeat: no-repeat;
            background-size: 25px 25px;
            width: 25px;
            display: inline-block;
            height: 25px;
            position: relative;
            top: 6px;
          }

          .ai-error {
            margin-left: 5px;
          }
        }
      }

      .time-slots-to-suggest-title {
        color: $darkBlue;
        margin: 10px 0 0;
        text-align: left;
        font-size: 14px;
        font-weight: bold;
      }

      .time-slots-to-suggest-outbound-count {
        color: red;
        font-size: 12px;
      }

      .time-slots-to-suggest-timezone {
        font-size: 15px;
      }

      .btn-success {
        margin-top: 40px;
      }
    }
  }

  .to-founders-message-div {
    font-weight: 400;
    font-size: 14px;
    color: white;
    margin: 5px -10px;
    padding: 20px 10px;
    background: #F00000;
  }

  .account {
    font-size: 16px;
    word-break: break-word;



    .no-account-detected {
      background: #f60;
      color: white;
      text-align: center;
      font-size: 16px;
      font-weight: 200;
      padding: 10px;

      .no-account-email {
        font-size: 12px;
        padding: 20px 0 0 0;
      }
    }

    .other-accounts {
      display: none;
      margin: 10px 0;

      .other-accounts-title {
        font-size: 14px;
        color: #aaa;
      }

      .other-accounts-list {
        border-top: 1px solid #ccc;
        border-left: 1px solid #ccc;
        border-right: 1px solid #ccc;

        .other-account {
          border-bottom: 1px solid #ccc;
          padding: 10px;

          .other-account-name {
            font-size: 14px;
          }

          .other-account-email {
            font-size: 12px;
          }
          .other-account-timezone {
            font-size: 12px;
          }
          .other-account-preferences, .other-account-current-notes, .other-account-info {
            font-size: 12px;
            margin: 5px 0;
          }
        }
      }
    }
  }

  .contacts {
    ul {
      list-style-type: none;
    }
    text-align: right;
    font-size: 12px;
  }

  .data {
    padding: 50px 0 0 0;

    textarea.constraints:read-only, textarea.constraints:disabled {
      background: #FB6F45;
      color: #ffffff;
      font-size: 15px;
      border: none;
      box-shadow: none;
      border-radius: 0;
      resize: none;
    }
  }

  .data-entry {

    .semi-data-entry {
      display: inline-block;
      width: 40%;
    }
    .data-entry-name {
      font-size: 11px;
      opacity: 0.8;
      margin: 10px 0 0 0;
    }
    .data-entry-value {
      //font-size: 14px;
      word-break: break-word;
      input {
        word-break: normal;
      }

    }
    #contacts-data-list {
      .contact {
        position: relative;
        height: 55px;
        padding: 5px 0 0 0;
      }
      input[type=checkbox] {

      }
      .attendee-name-and-email {
        position: absolute;
        width: 210px;
        top: 0;
        left: 20px;
        input#attendee_name {
          width: 100%;
        }
        .attendee-email {
          font-size: 12px;
          color: #999;
        }
      }
    }
    input#summary {
      &.warning {
        border-color: #d00;
      }
    }

    select#location_nature {
      margin-bottom: 5px;
    }

    .add-attendee-container {
      position: relative;
      margin: 10px 0 0 20px;

      input {
        padding-right: 65px;
        width: 100%;
      }
      .btn {
        position: absolute;
        right: 2px;
        top: 2px;
        width: 55px;
      }
    }

    .different-timezone-warner {
      background: #e00;
      color: white;
      text-align: center;
      margin: 10px -10px;
      padding: 20px;
      display: none;
    }

  }

  .creating-event-message {
    padding: 5px 0;
    line-height: 30px;
    text-align: center;
  }

  .save-info-container {
    display: none;
  }

  .dates-identification-panel {
    display: none;
  }

  .linear-form-entry {
    .validate-linear-form-entry-button {
      width: 100px;
      margin: 10px 0 10px 230px;
    }

    &.locale-selection {
      font-size: 15px;
    }

    &.appointment-type-selects {
      select {
        font-size: 15px;
      }
    }
  }

  .title-preferences-checkboxes {
    span {
      margin-left: 10px;
    }
    span:first-child {
      margin-left: 0px;
    }
  }

  .send-to-founders-container {
    background: #f0f0f0;
    margin: 10px -10px 0 -10px;
    text-align: center;
    padding: 10px;
    border-top: 1px solid #ddd;
    border-bottom: 1px solid #ddd;
  }

  .add-preference-panel {
    display: none;

    .add-preference-data-entry {

      .data-entry-name {
        font-size: 11px;
        opacity: 0.8;
        margin: 10px 0 0 0;
      }

      textarea#preference_to_add {
        height: 100px;
        width: 100%;
        resize: none;
        border: 1px solid #ccc;
      }
    }
  }

  .meeting-rooms-manager-wrapper {
    label {
      font-weight: 400;
    }
    .checkbox-header {
      margin-top: 5px;
      label {
        font-weight: 700;
      }
    }

    .meeting-rooms-warning-area {
      color: #EA5959;
      font-weight: bold;
    }

  }

  .restaurant-booking-manager-wrapper, .create-event-restaurant-booking-manager-wrapper {
    margin-top: 5px;

    .checkbox-header {
      margin-bottom: 5px;
      .sprite {
        background-image: url(<%= asset_path 'restaurant_booking_logo2x.png' %>);
        background-repeat: no-repeat;
        background-size: 20px 20px;
        width: 20px;
        display: inline-block;
        height: 20px;
        position: relative;
        top: 5px;
      }
    }

    .restaurant-booking-selection-area {
      .text {
        width: 25%;
      }
      select {
        width: 75%;
        display: inline-block;
      }
    }
  }

  .follow-up-select-threads-panel {
    .messages-thread-entry {
      border: 1px solid #ddd;
      padding: 10px;
      margin: 0 0 15px;
      background: #fafafa;

      input[type=checkbox] {
        margin: 0 5px 0 0;
      }
      .messages-thread-label {
        font-size: 15px;
      }

      textarea {
        height: 50px;
        width: 100%;
        resize: none;
        outline: none;
        border: 1px solid #ccc;
        margin: 10px 0 0 0;
      }
    }
  }

}

li.contact-autocomplete-item {
  .contact-autocomplete-name {
    font-size: 14px !important;
  }
  .contact-autocomplete-email {
    font-size: 12px !important;
  }
  .contact-autocomplete-company {
    font-size: 10px !important;
  }
  .contact-autocomplete-email-alias {
    font-size: 10px !important;
  }
}

.associate-to-account-container {
  border: 1px solid #ccc;
  padding: 10px;
  margin: 20px 0 0 0;
  input#account-email-input {
    font-size: 14px;
    width: 100%;
    padding: 5px;
    margin: 10px 0 20px 0;
  }
}

.locale-discrepancy-message {
  height: 30px;
  margin-top: 5px;
  display: none;
  #locale_discrepancy_sprite {
    background-image: url(<%= asset_path 'warning2x.png' %>);
    background-repeat: no-repeat;
    background-size: 30px 30px;
    width: 30px;
    display: inline-block;
    height: 30px;
  }
  #locale_discrepancy_text {
    font-size: 15px;
    color: $aiControlColor;
    position: relative;
    top: -1px;
    left: 5px;
    font-style: italic;
    font-weight: bold;

    .locale-ai-right-link {
      font-size: 14px;
      font-style: normal;
      font-weight: 400;
      margin-left: 5px;
      text-decoration: underline;
    }
  }

}
