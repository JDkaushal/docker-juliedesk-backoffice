<div class="meeting-rooms-manager-wrapper" ng-show="displayForm">
  <div>
    <div>
      <meeting-rooms-widget ng-repeat="widget in widgets" widget-data="widget" clients-list="clientsList" currently-choosen-rooms="currentlyChoosenRooms"/>
    </div>

    <div>
      <button ng-click="addWidget()" class="add-widget-btn">+ Ajouter Salle</button>
    </div>
  </div>
</div>